spring:
  cloud:
    config:
      server:
        bootstrap: true
        native:
          search-locations: file:/config/product-service/product-service.yml  # Путь к файлу конфигурации
        # Если бы вы использовали Git, то эта секция выглядела бы так:
        # git:
        #   uri: git@github.com:RylovK/nanomarket-config-repo.git
        #   private-key: ${GIT_SSH_PRIVATE_KEY}
        #   clone-on-start: true
        #   search-paths: config
  profiles:
    active: native  # Активный профиль
